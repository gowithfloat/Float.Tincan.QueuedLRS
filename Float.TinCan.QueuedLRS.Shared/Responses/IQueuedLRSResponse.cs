namespace Float.TinCan.QueuedLRS.Responses
{
    /// <summary>
    /// Just used to denote local LRS responses as such.
    /// Consumers can use type checking to see if an LRS resonse was from the queued LRS or the remote LRS.
    /// </summary>
    public interface IQueuedLRSResponse
    {
    }
}
